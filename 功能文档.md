# ESP32S3_PPP_V1 功能文档

## 版本信息

- 项目名称：`ESP32S3_PPP_V1`
- 当前版本：`V1.1`
- 目标平台：`ESP32-S3`
- 4G 模组：`Quectel EC200A`
- 软件框架：`ESP-IDF v6.0.1`

## 项目目标

本项目用于实现基于 `ESP32-S3 + EC200A` 的基础 4G 转 Wi-Fi 路由功能。

当前版本重点完成以下最小闭环：

- EC200A 通过 USB 接入 ESP32-S3
- 4G 模组完成 PPP 拨号并获取公网链路
- ESP32-S3 开启 Wi-Fi SoftAP
- Wi-Fi 客户端通过 SoftAP 共享 4G 网络访问外网
- 本地 WebUI 可查看状态并执行基础配置

## 已实现功能

### 1. 系统启动与模块初始化

- 启动后按顺序初始化运行状态、NVS、系统监控、Wi-Fi SoftAP、4G PPP、WebUI、LED 状态任务
- `main.c` 仅负责启动协调，不承载具体业务逻辑

### 2. Wi-Fi SoftAP 功能

- 默认热点名称：`EC200A`
- 默认热点密码：`12345678`
- 默认工作信道：`1`
- 支持 DHCP 分配地址
- 默认网关地址：`192.168.4.1`
- 支持运行时重启热点并应用新配置

### 3. 4G 模组 USB Host 接入

- ESP32-S3 作为 USB Host 识别 EC200A
- 打开 AT 控制口与 PPP 数据口
- 支持 CDC-ACM 数据收发
- 支持模块断开检测

### 4. PPP 拨号功能

- 通过 `AT+CGDCONT` 下发 APN
- 支持发起 PPP 拨号并等待 `CONNECT`
- PPP 成功后获取 IP 地址与 DNS
- 注册 PPP 错误事件，更新拨号状态和最近错误信息
- 当前默认 APN 已切换为中国移动：`cmnet`

### 5. PPP 拨号回退与恢复机制

- 优先尝试使用 PPP 口拨号
- PPP 口拨号失败时，自动回退到 AT 口拨号
- 拨号失败后执行恢复流程，重新准备后续重拨
- 新增 AT 握手重试机制，降低偶发 AT 超时带来的卡死概率
- 新增 PPP 口探测二次重试，给模组口状态稳定留出缓冲时间
- 新增 `+++` 退出数据模式保护间隔，避免模组残留在异常数据态
- 新增 `AT+CGATT?` 检查，确认分组域附着后再进入拨号流程

### 6. 4G 转 Wi-Fi 共享

- PPP 上线后自动设置默认外网接口
- 将 PPP 获取到的 DNS 同步给 SoftAP
- 启用 NAPT，实现 Wi-Fi 客户端通过 4G 上网

### 7. WebUI 本地管理页面

- 访问地址：`http://192.168.4.1/`
- 可显示系统运行时间、软件版本、内存、热点 IP、芯片温度、任务分布
- 可显示 4G 模组状态、拨号状态、PPP IP、DNS、SIM 状态、信号强度、驻网状态、运营商信息、当前 APN、最近错误
- 支持修改热点名称、密码、信道
- 支持修改 APN 配置
- 支持恢复默认 Wi-Fi 参数
- 支持触发 4G 重拨
- 支持 LED 状态开关
- 支持远程重启设备

### 8. 配置持久化

- Wi-Fi 参数保存到 NVS
- APN 配置保存到 NVS
- 设备重启后自动加载上次保存的配置

### 9. 系统监控

- 读取芯片温度
- 统计 CPU 主频
- 统计 FreeRTOS 任务总数
- 统计双核任务分布
- 当前监控刷新周期为 `5 秒`

### 10. LED 状态指示

- 支持 WS2812 单灯状态显示
- 使用不同颜色/阶段反映：
- Wi-Fi AP 状态
- Wi-Fi STA 接入数状态
- EC200A 硬件就绪状态
- PPP 链路状态

## 当前版本已完成的上板验证

本版本已在以下环境完成基础验证：

- ESP32-S3 调试串口：`COM25`
- EC200A 插入中国移动 SIM 卡
- SoftAP 名称：`EC200A`

已验证通过项目：

- 工程可成功编译
- 固件可成功烧录
- SoftAP 可被 Windows 主机连接
- WebUI 可正常打开
- PPP 拨号成功
- 成功获取 PPP 地址：`10.35.45.148`
- 成功获取 DNS：`211.140.11.66`
- APN 为：`cmnet`
- Wi-Fi 客户端已验证可经由 `192.168.4.1` 共享 4G 出口访问外网

## FreeRTOS 调度分析

### 当前任务与绑核策略

| 任务/线程 | 优先级 | 绑核 | 位置 | 设计理由 |
| --- | --- | --- | --- | --- |
| Wi-Fi driver task | 23 | Core0 | ESP-IDF Wi-Fi 内部任务 | Wi-Fi/LwIP 本身偏网络侧，放在 Core0 有利于减少跨核切换 |
| `usb_lib_task` | 20 | Core0 | `cellular_ppp.c` | USB Host 事件处理实时性要求最高，靠近 CDC 与 PPP 数据面最稳 |
| CDC ACM driver task | 19 | Core0 | `cellular_ppp.c` | 承接 USB CDC 数据收发，和 USB Host、蜂窝任务同核能减少同步抖动 |
| HTTP server task | 默认配置 | Core1 | `webui.c` | WebUI 属于本地管理面，放在应用核，避免和 PPP 链路抢网络核时间片 |
| `cellular_task` | 5 | Core0 | `cellular_ppp.c` | 负责 AT、拨号、PPP 状态维护，紧邻 USB/CDC 更合理 |
| `wifi_reconfigure_task` | 5 | Core1 | `wifi_ap.c` | 配置修改属于管理动作，不必占用网络核 |
| `device_restart_task` | 5 | Core1 | `webui.c` | 设备重启动作来自 WebUI 控制，放应用核更合适 |
| `led_status_task` | 4 | Core1 | `led_status.c` | LED 指示属于本地显示任务，不应干扰数据面 |
| `system_monitor_task` | 3 | Core1 | `system_monitor.c` | 纯后台监测任务，优先级最低，避免与 PPP 竞争 |

### 当前策略评估

| 项目 | 结论 | 说明 |
| --- | --- | --- |
| 网络相关任务集中 Core0 | 合理 | USB Host、CDC、蜂窝 PPP、Wi-Fi 网络栈同侧，减少跨核通信 |
| 本地 UI/辅助任务集中 Core1 | 合理 | WebUI、LED、监控属于低实时性任务，适合放应用核 |
| 高优先级分配 | 合理 | USB Host 与 CDC 优先级明显高于 PPP 业务任务，符合数据链路实时性要求 |
| 蜂窝业务优先级 | 基本合理 | `APP_TASK_PRIO_CELLULAR=5` 足够支撑当前基础功能，不建议在稳定前再上调 |
| 监控任务轮询周期 | 已优化 | 已从 `2 秒` 调整到 `5 秒`，降低了后台轮询对 PPP 的干扰 |

### 当前建议

- 维持现有 Core0/ Core1 分工，不建议在当前稳定版本上做大幅改核
- 保持 `usb_lib_task`、CDC 驱动、`cellular_task` 同核运行
- 继续把 WebUI/LED/监控类任务留在 Core1
- 后续若要扩展流量统计、日志采集、云端管理，再单独评估新的任务优先级

## 当前未实现或未完善功能

- 不包含 OTA 升级
- 不包含 HTTPS 或登录鉴权
- 不包含短信、电话、GNSS 等 EC200A 扩展业务
- 不包含流量统计、在线时长统计、速率统计
- 不包含远程云平台接入
- 不包含更复杂的多级异常自愈策略
- 不包含自动化测试与单元测试体系

## 任务与模块分工概览

- `main/app_config.*`：Wi-Fi/APN 配置与 NVS
- `main/app_state.*`：运行状态共享
- `main/wifi_ap.*`：SoftAP 建立与重配置
- `main/cellular_ppp.*`：EC200A USB Host、AT、PPP、NAPT
- `main/webui.*`：本地管理页面
- `main/system_monitor.*`：温度与任务分布监控
- `main/led_status.*`：LED 状态指示

## 发布说明

`V1.1` 版本定位为：

- 基础 4G PPP 拨号稳定版
- 基础 4G 转 Wi-Fi 可用版
- 带本地 WebUI 管理能力的最小可用版本
